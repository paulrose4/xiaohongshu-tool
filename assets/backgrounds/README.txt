Place a "独居女孩温馨出租屋" style background image here named cozy_room.jpg
(referenced by COMPOSITE_BG_PATH in .env / config.toml).

If absent, the visual node falls back to a procedurally generated warm gradient
so the pipeline still runs end-to-end.
